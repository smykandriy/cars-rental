# UML Class Diagram

The class diagram captures the domain models plus the core service/repository classes used to implement pricing, rental state transitions, and reporting.

See `class_diagram.puml` for the UML class diagram.
